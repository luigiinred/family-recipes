# Service specs

Cross-cutting behavior that spans multiple pages (auth, sync, imports, search indexing).

| Service spec | Status | Notes |
| -------------- | ------ | ----- |
| *(create `services/<name>.md` when needed)* | | |

Status: `planned`, `complete`, `broken`.
